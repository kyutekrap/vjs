import { BackgroundDefault, SectionDefault } from "../../../components/index.js";

new SectionDefault({
    children: [
        new BackgroundDefault({
            src: "",
            width: "100px",
            height: "100px"
        })._background
    ]
})